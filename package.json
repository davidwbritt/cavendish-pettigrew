{
  "name": "aptitude",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test test/",
    "build": "node build.mjs",
    "dev": "node dev-server.mjs"
  }
}
